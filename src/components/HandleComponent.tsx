const HandleComponent = () => {
  return (
    <div className="absolute -right-[0.1px] -bottom-[0.1px]">
      <div className="w-4 h-4 rounded-full shadow border bg-blue-950 border-zinc-200 transition hover:bg-primary" />
    </div>
  );
};

export default HandleComponent;
